# Real credentials — gitignored, never commit this file.

SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
# 587 = STARTTLS, so this stays "false" (paired with 465 it would need "true").
SMTP_SECURE=true
SMTP_USER=support@t2upgrade.com
SMTP_PASSWORD=Support@t2u

SMTP_FROM_EMAIL=support@t2upgrade.com
SMTP_FROM_NAME=T2Upgrade
SMTP_REPLY_TO=support@t2upgrade.com

# T2U public API — no key required, safe to expose client-side.
NEXT_PUBLIC_T2U_API_BASE=https://t2upgrade.com/api
